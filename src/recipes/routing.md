# Routing

Routing can be done with a combination of state and `switch`.

TODO
THIS IS UNFINISHED

```rust,ignore
struct Router;

impl Component for Router {
    State = String;
    Message = String;
}
```

```aml
text "hello"
    span "sausage"
```
